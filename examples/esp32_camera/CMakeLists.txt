# ESP-IDF project CMakeLists.txt for nanortc ESP32-P4 Camera example
#
# Build: cd examples/esp32_camera && idf.py set-target esp32p4 && idf.py build
# Flash: idf.py flash monitor

cmake_minimum_required(VERSION 3.16)

# nanortc is an extra component (parent repo root)
set(EXTRA_COMPONENT_DIRS "${CMAKE_CURRENT_SOURCE_DIR}/../..")

# Explicitly list all sdkconfig defaults.
# Setting SDKCONFIG_DEFAULTS overrides IDF's auto-detection, so we must
# include the target-specific file ourselves.
# board_manager.defaults is generated by gen_bmgr_config_codes.py and
# provides Peripheral/Device Support configs + camera sensor selection.
set(SDKCONFIG_DEFAULTS
    "${CMAKE_CURRENT_SOURCE_DIR}/sdkconfig.defaults"
)
set(_target_defaults "${CMAKE_CURRENT_SOURCE_DIR}/sdkconfig.defaults.${IDF_TARGET}")
if(EXISTS "${_target_defaults}")
    list(APPEND SDKCONFIG_DEFAULTS "${_target_defaults}")
endif()
set(_bmgr_defaults "${CMAKE_CURRENT_SOURCE_DIR}/components/gen_bmgr_codes/board_manager.defaults")
if(EXISTS "${_bmgr_defaults}")
    list(APPEND SDKCONFIG_DEFAULTS "${_bmgr_defaults}")
endif()

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(esp32_camera)
